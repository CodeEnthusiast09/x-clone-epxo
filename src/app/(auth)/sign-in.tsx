import { View, Text } from 'react-native';

export default function SignInScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold text-gray-900">Sign In</Text>
      <Text className="mt-2 text-sm text-gray-500">Clerk sign-in UI coming soon</Text>
    </View>
  );
}
